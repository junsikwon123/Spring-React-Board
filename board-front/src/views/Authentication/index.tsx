import React from 'react';
import './style.css'

// component : 인증화면 컴포넌트
const Authentication = () => {

    // render : 인증화면 컴포넌트 렌더링
    return (
        <div>

        </div>
    );
};

export default Authentication;